-- =============================================================================
-- Hayy – Migration 005 (intentionally a no-op placeholder)
--
-- Migration 005 was reserved during early planning but never authored;
-- the next change to ship was migration 006 (room types and creation
-- permissions). This file exists so the migration sequence is unbroken
-- for tools that expect dense numbering, and to document the gap for
-- anyone reading the migrations folder.
--
-- DO NOT delete this file. If you ever need to bump the next migration
-- number, start at 007.
--
-- This file is a no-op — running it has no effect.
-- =============================================================================

select 1 where false;
